# from django.test import SimpleTestCase
# from django.urls import reverse, resolve
# from dashboard.views import CreateProject, dashboard  

# class TestUrls(SimpleTestCase):
#     def test_create_project_url_resolves(self):
#         url = reverse('CreateProject')
#         self.assertEqual(resolve(url).func, CreateProject)

#     def test_dashboard_owner_url_resolves(self):
#         url = reverse('Owner_dashboard')
#         self.assertEqual(resolve(url).func, dashboard)
